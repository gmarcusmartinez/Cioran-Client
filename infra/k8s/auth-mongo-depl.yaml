apiVersion: apps/v1
kind: Deployment
metadata:
  name: cioran-auth-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cioran-auth-mongo
  template:
    metadata:
      labels:
        app: cioran-auth-mongo
    spec:
      containers:
        - name: cioran-auth-mongo
          image: mongo:4.4.2
---
apiVersion: v1
kind: Service
metadata:
  name: cioran-auth-mongo-srv
spec:
  selector:
    app: cioran-auth-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
